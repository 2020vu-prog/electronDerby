{
  "name": "svelte-derby-electron",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "yarn run build && node --test",
    "start": "electron ./out/electron/index.js",
    "timer": "yarn run build && yarn run start"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.4.0",
    "electron": "^44.0.0",
    "typescript": "^7.0.2"
  }
}
